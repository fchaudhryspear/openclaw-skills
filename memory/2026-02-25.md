
- **LaunchDaemon Troubleshooting & Resolution:** Successfully configured OpenClaw to start before login using a system-wide `LaunchDaemon` located at `/Library/LaunchDaemons/ai.openclaw.gateway.system.plist`. This involved extensive troubleshooting of "Input/output error" (Bootstrap failed: 5) with `launchctl`, verifying plist syntax with `plutil -lint`, resolving file existence issues, and manually removing conflicting `LaunchAgent` files. The gateway now starts correctly without user login.
- **GPT-4o Model Recognition Issue:** Encountered a persistent "Model 'openai/gpt-4o' is not allowed" error, despite correct `openclaw.json` configuration and multiple restarts/`doctor --fix` runs. This was identified as a potential bug within OpenClaw's caching or configuration loading.
- **OpenClaw Update & GPT-4o Resolution:** Upgraded OpenClaw to version `2026.2.24` using `openclaw update`. This update successfully resolved the `openai/gpt-4o` model recognition issue, and the model is now fully functional.
